name: Cron
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request POST \
          --url '${{ secrets.FRONT_END_URL }}/api/cron' \
          --header 'Authorization: Bearer ${{ secrets.API_SECRET }}'
